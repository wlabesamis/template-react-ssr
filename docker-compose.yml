version: '3'
services:
  react-ssr:
    build:
      dockerfile: ./Dockerfile
      context: ./
      args:
        WORKDIR: ${WORKDIR}
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: on-failure
